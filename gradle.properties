# 关闭 JVM 的校园代理（proxy-dku.oit.duke.edu:3128）
# 该代理会对 Maven Central 等外网仓库返回 403，导致依赖下载失败。
# 通过把代理主机设为空字符串，让 Gradle 直连外网。
systemProp.http.proxyHost=
systemProp.https.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyPort=
